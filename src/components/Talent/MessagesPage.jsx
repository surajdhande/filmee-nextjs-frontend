"use client";

import React from "react";
import TalentMessages from "@/components/dashboard/talent/TalentMessages";

export default function MessagesPage({ onBack }) {
  return <TalentMessages onBack={onBack} />;
}
