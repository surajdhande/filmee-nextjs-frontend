import SearchPage from "@/components/SearchPage/SearchPage";

export const metadata = {
  title: "Search Everything | Filmee",
  description: "Find projects, talent, investment opportunities, and more across the entire FilmConnect platform.",
};

export default function SearchRoute() {
  return <SearchPage />;
}
